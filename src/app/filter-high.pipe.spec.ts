import { FilterHighPipe } from './filter-high.pipe';

describe('FilterHighPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHighPipe();
    expect(pipe).toBeTruthy();
  });
});
